/**
 * 
 */

function initMemory(){
	//check for depression and age
}

function runHADSEvaluation(){
	var answers = $.makeArray($(".h_select"));
	console.log(answers.first().val());
	for(var i = 0; i < answers.length; i++){
		console.log(answers[i].val());
	}
}