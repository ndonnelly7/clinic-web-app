function CheckChannel() {
	$.ajax('/webrtceval.do', {
		method:'GET',
		dataType:'text',
		data: {
			type:"ChannelCheck"
		},
		success:function(response) {
			$("#infotext").append("<div>Server Received Request</div>");
		}
	});
}